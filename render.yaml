services:
  - type: web
    name: dot-sentinel-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn parser.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
